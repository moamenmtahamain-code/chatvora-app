version: '3.8'
services:
  lan-portal:
    build: ./backend
    ports:
      - '3000:3000'
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    volumes:
      - ./backend/public:/app/public
